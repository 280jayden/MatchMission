export type Organization = {
  ein: string;
  name: string;
  description: string;
  logoUrl: string;
  websiteUrl: string;
  profileUrl: string;
  location: string;
};

export type Tag = {
  tagImageUrl: string;
  title: string;
}