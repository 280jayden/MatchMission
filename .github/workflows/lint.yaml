name: Ruff Style Checker
on: push

jobs:
  check-style:
    runs-on: ubuntu-latest
    steps:

      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Run Ruff Linter
        uses: astral-sh/ruff-action@v4
        with:
          work-dir: "backend"